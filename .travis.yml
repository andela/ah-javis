language: python
python:
  - "3.7"
services:
  - postgresql
beforehscript:
  - psql -c 'create database testdb;' -U postgres
install: pip install -r requirements.txt
script:
  - python manage.py test
notifications:
  - email: false
